class Solution {
public:
    long long sumOfSeries(long long n) {
        return (n * (n + 1) / 2) * (n * (n + 1) / 2);
    }
};

// correct code but time limit exceeded using pow function in recursion

